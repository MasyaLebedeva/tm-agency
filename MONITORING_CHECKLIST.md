# Чек-лист проверки мониторинга бота

## ✅ Что уже сделано:
1. ✅ Файл `bot.py` обновлен с health-check endpoint
2. ✅ URL в мониторе изменен на `/health`

## 🔍 Что проверить сейчас:

### 1. Проверка health endpoint вручную
Откройте в браузере:
```
https://gigtest-bot-new.onrender.com/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "message": "Бот работает нормально",
  "timestamp": "2025-01-XX..."
}
```

### 2. Проверка корневого пути
Откройте:
```
https://gigtest-bot-new.onrender.com/
```

Должен вернуться тот же JSON.

### 3. Проверка в мониторе
- Подождите 5-10 минут после замены файла и URL
- Статус должен измениться с "Preparing" на "Up"
- Uptime должен начать считаться с 0%

### 4. Если статус все еще красный:

#### Вариант A: Render не перезапустился
- Зайдите в панель Render
- Нажмите "Manual Deploy" → "Deploy latest commit"
- Подождите завершения деплоя

#### Вариант B: Проверьте логи Render
- В панели Render откройте логи
- Убедитесь, что нет ошибок при запуске
- Должны быть строки:
  - "Инициализация бота @gigtestibot..."
  - "Запуск сервера на порту..."

#### Вариант C: Проверьте переменные окружения в Render
Убедитесь, что установлены:
- `TELEGRAM_TOKEN`
- `ADMIN_IDS`
- `WEBHOOK_URL` (опционально, по умолчанию используется `https://gigtest-bot-new.onrender.com`)

## 📝 Дополнительно:

Если монитор показывает ошибку:
1. Проверьте, какой именно статус код возвращается
2. Попробуйте изменить URL в мониторе на корневой `/` вместо `/health`
3. Убедитесь, что бот действительно работает в Telegram

---

**Время ожидания:** После деплоя на Render может потребоваться 1-2 минуты для перезапуска сервиса.

