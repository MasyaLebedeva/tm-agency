# 🔄 Миграция на мульти-бот (ROI + Gigtest)

## Что нужно сделать

### Шаг 1: Обнови переменные окружения в Railway

Открой Railway → проект → **"Variables"** и сделай следующее:

#### Переименуй текущие переменные ROI бота (добавь префикс BOT1_):

**Старые → Новые:**
```
TELEGRAM_TOKEN → BOT1_TOKEN
CHANNEL_ID → BOT1_CHANNEL_ID
CHANNEL_LINK → BOT1_CHANNEL_LINK
ADMIN_IDS → BOT1_ADMIN_IDS
GOOGLE_SHEETS_LINK → BOT1_GOOGLE_SHEETS_LINK
VIDEO_LINK → BOT1_VIDEO_LINK
```

#### Добавь переменные для Gigtest бота (BOT2_):

```
BOT2_TOKEN=токен_бота_gigtest
BOT2_CHANNEL_ID=-1001324681912
BOT2_CHANNEL_LINK=https://t.me/lebedevamariiatgm
BOT2_ADMIN_IDS=252904
BOT2_GOOGLE_DOC_LINK=https://docs.google.com/document/d/1wRpzasug5kSagNZgtG2QlSRMyK-7PP3ZYvNcejoDkoo/edit?usp=sharing
```

**Важно:** Для Gigtest бота используется `BOT2_GOOGLE_DOC_LINK` (не GOOGLE_SHEETS_LINK)!

#### Общие переменные (оставь как есть):
```
WEBHOOK_URL=https://web-production-150b4.up.railway.app
PORT=8080
```

### Шаг 2: Procfile уже обновлён

Procfile уже настроен на `web: python multi_bot.py`

### Шаг 3: Отправь изменения в репозиторий

```bash
git add multi_bot.py requirements.txt Procfile MIGRATE_TO_MULTI_BOT.md
git commit -m "Migrate to multi-bot: combine ROI and Gigtest bots"
git push origin main
```

### Шаг 4: Проверь логи Railway

После деплоя должны быть сообщения:
```
✅ Бот BOT1 успешно зарегистрирован
✅ Бот BOT2 успешно зарегистрирован
✅ Webhook для BOT1 установлен
✅ Webhook для BOT2 установлен
```

## Как это работает

1. **BOT1** (ROI калькулятор):
   - Выдаёт Google таблицу и видео после подписки
   - Имеет меню с кнопками
   - База данных: `bot1.db`

2. **BOT2** (Gigtest):
   - Выдаёт Google документ с ответами на тесты после подписки
   - Простая структура
   - База данных: `bot2.db`

3. **Webhook URL**: `https://домен.up.railway.app/webhook/{TOKEN}`
   - Telegram автоматически определяет бота по токену в URL
   - Каждый бот обрабатывается независимо

## Проверка работы

1. Открой первого бота (ROI калькулятор) → отправь `/start` → должен работать
2. Открой второго бота (Gigtest) → отправь `/start` → должен работать
3. Оба бота работают независимо в одном сервисе!

## Преимущества

✅ **Экономия часов**: Один сервис вместо двух  
✅ **Простое управление**: Все боты в одном месте  
✅ **Общие ресурсы**: Один процесс, одна память  
✅ **Легко масштабировать**: Добавь больше ботов без новых сервисов

## Откат (если что-то пошло не так)

1. Верни Procfile: `web: python bot.py`
2. Верни старые переменные окружения (без префиксов)
3. Закоммить и запушь

## Важные моменты

- Каждый бот имеет свою БД (`bot1.db`, `bot2.db`)
- Переменные окружения должны иметь префиксы `BOT1_` и `BOT2_`
- Для Gigtest бота используется `GOOGLE_DOC_LINK` (не GOOGLE_SHEETS_LINK)
- Webhook URL остаётся тем же - Telegram автоматически использует правильный токен

