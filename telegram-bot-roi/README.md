# Telegram Bot - ROI Calculator Lead Magnet

Бот для раздачи лид-магнита (калькулятор ROI) в обмен на подписку на Telegram-канал.

## 🚀 Возможности

- ✅ Проверка подписки на канал
- ✅ Автоматическая выдача лид-магнита после подписки
- ✅ Отслеживание источников трафика (UTM-метки)
- ✅ Аналитика по пользователям и конверсиям
- ✅ Админ-панель со статистикой
- ✅ Поддержка deep links с параметрами

## 📋 Требования

- Python 3.8+
- Telegram Bot Token (получить у @BotFather)
- Telegram канал для подписки
- Google таблица (шаблон калькулятора ROI)
- Видео-инструкция (опционально)

## 🔧 Установка

1. **Клонируйте или скопируйте файлы бота**

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте переменные окружения:**
   - Скопируйте `.env.example` в `.env`
   - Заполните все необходимые параметры

4. **Создайте Google таблицу:**
   - Используйте файл `ROI_Calculator_Template_GoogleSheets.csv` из проекта
   - Загрузите в Google Sheets
   - Сделайте публичной (или с доступом по ссылке)
   - Скопируйте ссылку в `GOOGLE_SHEETS_LINK`

5. **Подготовьте видео-инструкцию:**
   - Загрузите на YouTube, Vimeo или другой сервис
   - Скопируйте ссылку в `VIDEO_LINK`

## 🎯 Настройка

### Получение токена бота

1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

### Получение ID канала

1. Добавьте бота в канал как администратора
2. Используйте @userinfobot или @getidsbot
3. Или используйте API: `https://api.telegram.org/bot<TOKEN>/getUpdates`
4. Скопируйте ID канала в `.env` (формат: `-1001234567890`)

### Настройка Google таблицы (ШАБЛОН)

**Важно:** CSV файл нужен только для создания шаблона один раз. Пользователи создают копию и работают со своими данными!

1. Откройте `ROI_Calculator_Template_GoogleSheets.csv`
2. Импортируйте в Google Sheets (Файл → Импортировать)
3. Настройте доступ: "Настройки доступа" → "Все, у кого есть ссылку" → "Просматриватель"
4. Скопируйте ссылку на шаблон в `.env`
5. Пользователи будут открывать шаблон и создавать копию (Файл → Создать копию) для работы

## 🚀 Запуск

### Локальная разработка (Polling)

Для разработки можно использовать polling вместо webhook:

```python
# В конце bot.py замените web.run_app на:
from aiogram import executor
executor.start_polling(dp, skip_updates=True)
```

### Продакшен (Webhook)

1. **Разверните на сервере** (например, Render, Heroku, VPS)

2. **Настройте webhook:**
   - Укажите `WEBHOOK_URL` в `.env`
   - Бот автоматически установит webhook при запуске

3. **Запустите:**
```bash
python bot.py
```

Или с помощью gunicorn:
```bash
gunicorn bot:app --bind 0.0.0.0:10000
```

## 📊 Использование

### Для пользователей

1. Пользователь переходит по ссылке на бота
2. Бот просит подписаться на канал
3. После подписки пользователь получает:
   - Ссылку на Google таблицу
   - Ссылку на видео-инструкцию
   - Дополнительные материалы

### Deep Links с UTM-метками

Используйте ссылки с параметрами для отслеживания источников:

```
https://t.me/your_bot?start=utm_source=article&utm_medium=blog&utm_campaign=roi_calculator
```

Бот автоматически сохранит эти параметры в базу данных.

### Админ-панель

Отправьте `/admin` боту для доступа к статистике:
- Общее количество пользователей
- Количество подписчиков
- Конверсия
- Статистика по источникам

## 🔍 Аналитика

Бот автоматически собирает:
- Источник перехода (UTM-метки)
- Время подписки
- Активность пользователей
- Конверсию в подписки

Все данные хранятся в SQLite базе данных `bot.db`.

## 📝 Структура базы данных

- **users** - информация о пользователях
- **stats** - статистика действий
- **reminders** - напоминания (для будущих функций)

## 🎨 Кастомизация

### Изменение текстов

Найдите в `bot.py`:
- `welcome_text` - приветственное сообщение
- `lead_magnet_text` - сообщение с лид-магнитом
- Другие текстовые сообщения

### Добавление функций

Бот легко расширяется:
- Добавьте новые обработчики команд
- Создайте новые callback handlers
- Расширьте базу данных

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте токен в `.env`
2. Убедитесь, что бот запущен
3. Проверьте логи на ошибки

### Не работает проверка подписки

1. Убедитесь, что бот добавлен в канал как администратор
2. Проверьте правильность `CHANNEL_ID` (должен начинаться с `-100`)
3. Проверьте права бота в канале

### Webhook не работает

1. Проверьте доступность `WEBHOOK_URL`
2. Убедитесь, что порт открыт
3. Проверьте SSL сертификат (нужен HTTPS)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все переменные окружения установлены
3. Проверьте права доступа к каналу и таблице

## 🔐 Безопасность

- Никогда не публикуйте `.env` файл
- Храните токены в секрете
- Регулярно обновляйте зависимости
- Используйте HTTPS для webhook

## 📈 Планы развития

- [ ] Автоматические напоминания неподписавшимся
- [ ] Реферальная система
- [ ] Квиз для персонализации
- [ ] Интеграция с CRM
- [ ] Экспорт статистики

---

**Создано для T&M Agency** 🚀

