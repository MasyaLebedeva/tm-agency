# 🚂 Деплой на Railway.app

## Пошаговая инструкция

### 1. Регистрация на Railway

1. Перейди на [railway.app](https://railway.app)
2. Нажми **"Start a New Project"** или **"Login"**
3. Войди через GitHub (рекомендуется) или email

### 2. Создание проекта

1. После входа нажми **"New Project"**
2. Выбери **"Deploy from GitHub repo"**
3. Если репозиторий не подключен:
   - Нажми **"Configure GitHub App"**
   - Выбери репозиторий `telegram-bot-roi`
   - Разреши доступ
4. Выбери репозиторий `telegram-bot-roi`
5. Railway автоматически определит Python проект

### 3. Настройка переменных окружения

1. В проекте Railway открой вкладку **"Variables"**
2. Добавь все переменные из `.env`:

```
TELEGRAM_TOKEN=твой_токен_бота
CHANNEL_ID=-1001234567890
CHANNEL_LINK=https://t.me/твой_канал
ADMIN_IDS=252904
GOOGLE_SHEETS_LINK=https://docs.google.com/spreadsheets/d/...
VIDEO_LINK=https://youtu.be/...
WEBHOOK_URL=
```

**Важно:** `WEBHOOK_URL` оставь пустым! Railway автоматически установит его после деплоя.

### 4. Получение URL для webhook

После деплоя:

1. Открой вкладку **"Settings"** в проекте
2. Найди секцию **"Domains"**
3. Railway автоматически создаст домен вида: `telegram-bot-roi-production.up.railway.app`
4. Скопируй этот URL
5. Вернись в **"Variables"**
6. Обнови `WEBHOOK_URL`:
   ```
   WEBHOOK_URL=https://telegram-bot-roi-production.up.railway.app/webhook/твой_токен
   ```
   Где `твой_токен` - это значение `TELEGRAM_TOKEN`

### 5. Проверка деплоя

1. Открой вкладку **"Deployments"**
2. Дождись успешного деплоя (зелёная галочка)
3. Открой вкладку **"Logs"** для просмотра логов
4. Проверь, что бот запустился и webhook установлен

### 6. Проверка работы бота

1. Открой Telegram
2. Найди своего бота
3. Отправь `/start`
4. Проверь, что бот отвечает

## 🔧 Настройки Railway

### Build Command (если нужно)
Railway автоматически определит Python проект, но можно указать явно:
```
pip install -r requirements.txt
```

### Start Command
Railway использует `Procfile`, который уже настроен:
```
web: python bot.py
```

### Python Version
Railway автоматически определит версию из `requirements.txt` или можно создать `runtime.txt`:
```
python-3.11.8
```

## 📊 Мониторинг

- **Logs** - просмотр логов в реальном времени
- **Metrics** - использование ресурсов
- **Deployments** - история деплоев

## 🔄 Обновление бота

1. Сделай изменения в коде
2. Закоммить и запушь в GitHub:
   ```bash
   git add .
   git commit -m "Update bot"
   git push origin main
   ```
3. Railway автоматически задеплоит изменения

## ⚠️ Важные моменты

1. **Бесплатный план:** 500 часов/месяц (достаточно для одного бота)
2. **База данных:** SQLite файл будет храниться в файловой системе (может удаляться при перезапуске)
3. **Webhook:** Railway автоматически управляет SSL сертификатами
4. **Порт:** Railway автоматически устанавливает переменную `PORT`

## 🆘 Решение проблем

### Бот не отвечает
1. Проверь логи в Railway
2. Убедись, что `WEBHOOK_URL` установлен правильно
3. Проверь, что `TELEGRAM_TOKEN` правильный

### Ошибка при деплое
1. Проверь логи сборки
2. Убедись, что все зависимости в `requirements.txt`
3. Проверь синтаксис Python кода

### Webhook не устанавливается
1. Проверь, что `WEBHOOK_URL` содержит полный URL с токеном
2. Проверь логи бота - там будет информация о webhook

## 📝 Чек-лист

- [ ] Зарегистрирован на Railway
- [ ] Создан проект из GitHub репозитория
- [ ] Добавлены все переменные окружения
- [ ] Деплой успешно завершён
- [ ] Получен URL для webhook
- [ ] Установлен `WEBHOOK_URL` в переменных окружения
- [ ] Бот отвечает в Telegram
- [ ] Проверена админ-панель `/admin`

## 🎉 Готово!

Твой бот теперь работает на Railway.app! 🚂

