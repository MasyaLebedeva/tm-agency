# 📋 Пошаговая инструкция: Как импортировать CSV в Google Sheets

## ⚠️ Важно: Правильный способ импорта

Google Sheets может не распознать файл, если просто открыть его. Нужно использовать **ИМПОРТ**.

## Шаг 1: Откройте Google Sheets
1. Перейдите на https://sheets.google.com
2. Войдите в свой аккаунт Google

## Шаг 2: Создайте новую таблицу
- Нажмите на **"+"** (Пустой файл) или **"Blank"**

## Шаг 3: Импортируйте файл
1. В меню сверху нажмите **"Файл"** (File)
2. Выберите **"Импортировать"** (Import)
3. Нажмите на вкладку **"Загрузить"** (Upload)
4. Нажмите **"Выбрать файл с устройства"** или перетащите файл `ROI_Calculator_Final.csv`

## Шаг 4: Настройки импорта (ВАЖНО!)
После загрузки файла появится окно настроек:

1. **Разделитель:** Выберите **"Запятая"** (Comma)
2. **Преобразовать текст в числа, даты и формулы:** ✅ **Включите** (Convert text to numbers, dates, and formulas)
3. Нажмите **"Импортировать данные"** (Import data)

## Шаг 5: Добавьте формулы вручную

После импорта нужно добавить формулы в ячейки:

### В столбец E (Итого по услугам):
- **E5:** `=IF(C5="Да";B5*D5;0)`
- **E6:** `=IF(C6="Да";B6*D6;0)`
- **E7:** `=IF(C7="Да";B7*D7;0)`
- **E8:** `=IF(C8="Да";B8*D8;0)`
- **E9:** `=IF(C9="Да";B9*D9;0)`
- **E10:** `=IF(C10="Да";B10*D10;0)`
- **E11:** `=IF(C11="Да";B11*D11;0)`
- **E12:** `=IF(C12="Да";B12*D12;0)`

### Итоговая сумма (E13):
- `=SUM(E5:E12)`

### Конверсия (B21):
- `=IF(B19<>0;B20/B19*100;0)`

### Результаты (столбец B):
- **B23:** `=E13`
- **B24:** `=B18`
- **B25:** `=B20*B18`
- **B26:** `=IF(E13<>0;(B25-E13)/E13*100;0)`
- **B27:** `=B25-E13`

---

## 🔄 Альтернатива: Создать таблицу с нуля (5 минут)

Если импорт не работает, создайте таблицу вручную:

1. Откройте Google Sheets
2. Создайте новую таблицу
3. Скопируйте данные из файла `ROI_Calculator_Final.csv` (откройте его в текстовом редакторе)
4. Вставьте в Google Sheets
5. Добавьте формулы вручную (см. выше)

---

## ❓ Если все еще не работает

Попробуйте:
1. Открыть файл в **Excel** или **Numbers** (на Mac)
2. Сохранить как **Excel файл (.xlsx)**
3. Загрузить Excel файл в Google Sheets

Или напишите мне, и я создам версию в другом формате!

